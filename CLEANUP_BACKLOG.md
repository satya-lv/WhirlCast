# Deferred Cleanup Items — Batch Later

Small, low-risk items noticed during the build that don't need to interrupt the main phased work. Tackle these together in one pass once the core Supply Planning module is complete (after Step 3.7).

- [ ] Remove/replace "Powered by DecisionPoint Analytics" footer text — appears at the bottom of every page, leftover branding from original WhirlCast template, not relevant to the current platform identity. Decide what (if anything) should replace it, or just remove it.
